<?xml version="1.0" encoding="utf-8"?>
<!-- Permit cleartext HTTP only to loopback / emulator-host dev addresses, so a
     local test server (reached over `adb reverse tcp:8443 tcp:8443`, where
     http://localhost is also a WebRTC-secure context) can be used during
     development. Every real server stays https-only. (Sozvon) -->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false"/>
</network-security-config>
